// Dev mock orders tests removed.
// These tests depended on development-only mock endpoints that are
// intentionally removed from the application. Keeping them causes
// accidental exposure of dev functionality.

describe.skip('Dev Mock Order API - removed', () => {
  test('dev mock order tests were removed as part of cleanup', () => {
    expect(true).toBe(true);
  });
});
